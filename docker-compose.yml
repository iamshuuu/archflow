services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: archflow
    ports:
      - "5432:5432"
    volumes:
      - archflow_pgdata:/var/lib/postgresql/data

volumes:
  archflow_pgdata:
